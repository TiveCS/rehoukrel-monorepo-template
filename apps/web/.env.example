# Environment
NODE_ENV=development

# Client-side environment variables (exposed to browser)
# Must be prefixed with VITE_ to be accessible in the client
# VITE_API_URL=http://localhost:8080
# VITE_APP_NAME=Rehoukrel
